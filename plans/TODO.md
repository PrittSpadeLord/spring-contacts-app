1. Set up tests for the remaining services, then maybe even for controllers and other classes. Need more research on this.
2. Set up Docker with a docker compose to run a `Dockerfile.test` before the real `Dockerfile` with necessary environmental variables.
3. Write up plan for "Logging in" functionality and implement it.